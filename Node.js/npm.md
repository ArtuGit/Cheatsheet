## Update all packages

Install:
npm install -g npm-check-updates

Check updates (current project):
ncu

Check global updates:
ncu -g 

Update:
ncu -u

Update, except:
ncu -x <moduleName>

Filter:
ncu -f mocha